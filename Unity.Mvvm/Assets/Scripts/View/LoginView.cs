using UnityEngine;
using ProjectCore;

public class LoginView : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        var q = Class1.GO();

        Debug.Log(q);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
